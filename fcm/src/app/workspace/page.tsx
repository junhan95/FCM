import Link from 'next/link'
import { requireSession } from '@/lib/auth/guard'
import { portalLang } from '@/lib/portal-lang'
export default async function Workspace() {
  const session = await requireSession(); const ko = await portalLang() === 'ko'
  return <main className="workspace-main"><div className="workspace-intro"><p className="portal-eyebrow">YOUR FRANKONIA WORKSPACE</p><h1>{ko ? '어떤 작업을 시작할까요?' : 'Where would you like to start?'}</h1><p>{ko ? `${session.displayName}님, 환영합니다.` : `Welcome back, ${session.displayName}.`} <span className="site-tag">{session.siteCode}</span></p></div><div className="module-grid"><Link className="module-card" href="/workspace/customer"><img src="/assets/headquarters.webp" alt="Frankonia headquarters"/><div className="module-copy"><span className="portal-eyebrow">01 / CUSTOMER COMMUNICATION</span><h2>Frankonia<br/>Customer Management</h2><p>{ko ? '등록 고객과의 관계를 이어가는 메일·뉴스 커뮤니케이션 플랫폼입니다.' : 'Your platform for sharing emails and news with registered Frankonia customers.'}</p><span className="module-cta">{ko ? '고객 관리 선택' : 'Select Customer Management'} <b>→</b></span></div></Link><Link className="module-card" href="/workspace/fct"><img src="/assets/chamber.webp" alt="Frankonia anechoic chamber"/><div className="module-copy"><span className="portal-eyebrow">02 / CHAMBER QUOTATION</span><h2>Frankonia<br/>Calculation Table</h2><p>{ko ? '챔버 구성, 부품 선정, 가격 계산을 하나의 견적 작업으로 연결합니다.' : 'Bring chamber configuration, component selection and pricing into one quotation workflow.'}</p><span className="module-cta">{ko ? 'FCT 설정하기' : 'Configure FCT'} <b>→</b></span></div></Link></div></main>
}
